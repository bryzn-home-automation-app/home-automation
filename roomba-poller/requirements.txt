# Roomba V4 / "Prime" cloud client — beta. Pinned to the field-tested tag.
# Requires Python 3.11+. Installs from git, so the image needs `git` present.
roombapy-prime @ git+https://github.com/johnnyh1975/roombapy-prime.git@v0.3.0b7

# Postgres driver (v3). [binary] pulls a prebuilt wheel — no libpq build deps.
psycopg[binary]==3.2.3
