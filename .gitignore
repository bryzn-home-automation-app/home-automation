# Java
target/
*.class
*.jar
*.war
!.mvn/wrapper/maven-wrapper.jar
.mvn/

# Node
node_modules/
dist/
.vite/

# IDE
.idea/
*.iml
.vscode/
*.swp
*.swo
*~

# Docker
.env
docker-compose.override.yml
# Local-only NUC infra (Caddy reverse-proxy config). The docker-compose.yml
# edits that add caddy/adguard are kept local via `git update-index
# --skip-worktree docker-compose.yml` on the NUC (a tracked file can't be
# gitignored).
caddy/

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Logs
*.log
logs/

# Integration temp files (deleted after processing)
playwright-traces/
screenshots/
green-button-downloads/

# Build
frontend/build/
*.tsbuildinfo
